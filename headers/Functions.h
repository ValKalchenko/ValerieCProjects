// pre-processor directives 

// create macro expression (constant)
// convert a temp from celsius to fahrenheit
#define FAHRENHEIT(temp) (temp * 1.8 + 32)

// include C header files
#include <stdio.h>
#include <string.h>

// functions declarations
void congradulate1(char *, char *, char *);
void congradulate2(char *, char *, char *);
float fahrenheitFromCelsius(float);